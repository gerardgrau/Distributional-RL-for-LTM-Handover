# notes/

Working notes that live outside the `docs/` set (the latter is intended for
reference material). Files here are scratch / project-management documents.

| File | Purpose | Tracked? |
|---|---|---|
| `tasks.md` | Live to-do list and calibration history (kept up to date) | yes |
| `conversation-summary.md` | Snapshot of in-progress threads with mentor | no (gitignored) |
| `meeting-notes.md` | Raw notes from the project meetings | no (gitignored) |

The two untracked files exist only on your local copy. The structured docs
(architecture, parity audit, future improvements) belong in `docs/`.
